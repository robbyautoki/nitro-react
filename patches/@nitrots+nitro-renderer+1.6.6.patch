diff --git a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/RoomObjectVisualizationFactory.ts b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/RoomObjectVisualizationFactory.ts
index 671cf7f..a14673f 100644
--- a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/RoomObjectVisualizationFactory.ts
+++ b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/RoomObjectVisualizationFactory.ts
@@ -219,7 +219,7 @@ export class RoomObjectVisualizationFactory implements IRoomObjectVisualizationF
             return null;
         }
 
-        if((visualizationData instanceof AvatarVisualizationData) || (visualizationData instanceof FurnitureMannequinVisualizationData))
+        if((visualizationData instanceof AvatarVisualizationData) || (visualizationData instanceof FurnitureMannequinVisualizationData) || (visualizationData instanceof PetVisualizationData))
         {
             visualizationData.avatarManager = Nitro.instance.avatar;
         }
diff --git a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/MovingObjectLogic.ts b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/MovingObjectLogic.ts
index ac3907b..be705da 100644
--- a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/MovingObjectLogic.ts
+++ b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/MovingObjectLogic.ts
@@ -15,6 +15,14 @@ export class MovingObjectLogic extends RoomObjectLogicBase
     private _changeTime: number;
     private _updateInterval: number;
 
+    // v9: Dynamic interval measurement
+    private _lastMoveTime: number;
+    private _measuredInterval: number;
+
+    // CSP v8: Simple one-step prediction
+    private _isPredicting: boolean;
+    private _predictionTarget: Vector3d;
+
     constructor()
     {
         super();
@@ -26,11 +34,18 @@ export class MovingObjectLogic extends RoomObjectLogicBase
         this._lastUpdateTime = 0;
         this._changeTime = 0;
         this._updateInterval = MovingObjectLogic.DEFAULT_UPDATE_INTERVAL;
+
+        this._lastMoveTime = 0;
+        this._measuredInterval = MovingObjectLogic.DEFAULT_UPDATE_INTERVAL;
+
+        this._isPredicting = false;
+        this._predictionTarget = new Vector3d();
     }
 
     protected onDispose(): void
     {
         this._liftAmount = 0;
+        this._isPredicting = false;
 
         super.onDispose();
     }
@@ -49,7 +64,6 @@ export class MovingObjectLogic extends RoomObjectLogicBase
                 if(this._liftAmount !== locationOffset.z)
                 {
                     this._liftAmount = locationOffset.z;
-
                     model.setValue(RoomObjectVariable.FURNITURE_LIFT_AMOUNT, this._liftAmount);
                 }
             }
@@ -58,26 +72,45 @@ export class MovingObjectLogic extends RoomObjectLogicBase
                 if(this._liftAmount !== 0)
                 {
                     this._liftAmount = 0;
-
                     model.setValue(RoomObjectVariable.FURNITURE_LIFT_AMOUNT, this._liftAmount);
                 }
             }
         }
 
+        // CSP v8: Timeout prediction if server doesn't respond within 2 intervals
+        if(this._isPredicting)
+        {
+            const elapsed = this.time - this._changeTime;
+
+            if(elapsed > this._updateInterval * 2)
+            {
+                this._isPredicting = false;
+                this._locationDelta.x = 0;
+                this._locationDelta.y = 0;
+                this._locationDelta.z = 0;
+                this.object.setLocation(this._location);
+
+                const m = this.object && this.object.model;
+                if(m) m.setValue(RoomObjectVariable.FIGURE_POSTURE, 'std');
+            }
+        }
+
         if((this._locationDelta.length > 0) || locationOffset)
         {
             const vector = MovingObjectLogic.TEMP_VECTOR;
 
-            let difference = (this.time - this._changeTime);
+            // v9: Use measured interval for interpolation speed
+            const interval = this._measuredInterval;
 
-            if(difference === (this._updateInterval >> 1)) difference++;
+            let difference = (this.time - this._changeTime);
 
-            if(difference > this._updateInterval) difference = this._updateInterval;
+            if(difference < 0) difference = 0;
+            if(difference > interval) difference = interval;
 
             if(this._locationDelta.length > 0)
             {
                 vector.assign(this._locationDelta);
-                vector.multiply((difference / this._updateInterval));
+                vector.multiply((difference / interval));
                 vector.add(this._location);
             }
             else
@@ -89,8 +122,10 @@ export class MovingObjectLogic extends RoomObjectLogicBase
 
             this.object.setLocation(vector);
 
-            if(difference === this._updateInterval)
+            if(difference === interval)
             {
+                this._location.add(this._locationDelta);
+
                 this._locationDelta.x = 0;
                 this._locationDelta.y = 0;
                 this._locationDelta.z = 0;
@@ -107,22 +142,161 @@ export class MovingObjectLogic extends RoomObjectLogicBase
         if(object) this._location.assign(object.getLocation());
     }
 
+    // CSP v8.1: Predict one step — allowed even during movement
+    public startPredictedMove(targetX: number, targetY: number, targetZ: number): void
+    {
+        if(!this.object) return;
+
+        // v8.1: If currently moving, snap _location to nearest tile from visual position
+        if(this._locationDelta.length > 0)
+        {
+            const interval = this._measuredInterval;
+            let progress = (this.time - this._changeTime) / interval;
+            if(progress > 1) progress = 1;
+
+            this._location.x = Math.round(this._location.x + this._locationDelta.x * progress);
+            this._location.y = Math.round(this._location.y + this._locationDelta.y * progress);
+            this._location.z = this._location.z + this._locationDelta.z * progress;
+
+            this._locationDelta.x = 0;
+            this._locationDelta.y = 0;
+            this._locationDelta.z = 0;
+        }
+
+        this._isPredicting = true;
+        this._predictionTarget.x = targetX;
+        this._predictionTarget.y = targetY;
+        this._predictionTarget.z = targetZ;
+
+        this._changeTime = this.time;
+        this._locationDelta.x = targetX - this._location.x;
+        this._locationDelta.y = targetY - this._location.y;
+        this._locationDelta.z = targetZ - this._location.z;
+
+        const dx = Math.sign(this._locationDelta.x);
+        const dy = Math.sign(this._locationDelta.y);
+
+        if(dx === 0 && dy === 0) return;
+
+        const model = this.object && this.object.model;
+        if(model) model.setValue(RoomObjectVariable.FIGURE_POSTURE, 'mv');
+
+        let dir = 0;
+        if(dx === 0 && dy === -1) dir = 0;
+        else if(dx === 1 && dy === -1) dir = 1;
+        else if(dx === 1 && dy === 0) dir = 2;
+        else if(dx === 1 && dy === 1) dir = 3;
+        else if(dx === 0 && dy === 1) dir = 4;
+        else if(dx === -1 && dy === 1) dir = 5;
+        else if(dx === -1 && dy === 0) dir = 6;
+        else if(dx === -1 && dy === -1) dir = 7;
+        this.object.setDirection(new Vector3d(dir * 45));
+    }
+
+    // CSP v8: NO super.processUpdateMessage() — we manage location ourselves to avoid 1-frame teleport
     public processUpdateMessage(message: IRoomObjectUpdateMessage): void
     {
-        if(!message) return;
+        if(!message || !this.object) return;
+
+        if(this._isPredicting && message instanceof ObjectMoveUpdateMessage)
+        {
+            this._isPredicting = false;
+
+            if(message.targetLocation)
+            {
+                const matchX = Math.round(message.targetLocation.x) === Math.round(this._predictionTarget.x);
+                const matchY = Math.round(message.targetLocation.y) === Math.round(this._predictionTarget.y);
 
-        super.processUpdateMessage(message);
+                if(matchX && matchY)
+                {
+                    if(message.direction) this.object.setDirection(message.direction);
+
+                    // v9: Track timing even for confirmed predictions
+                    this._trackMoveInterval();
+
+                    return;
+                }
+            }
 
-        if(message.location) this._location.assign(message.location);
+            // Server disagrees — snap to server position and start new interpolation
+            this._location.assign(message.location);
+
+            this._locationDelta.x = 0;
+            this._locationDelta.y = 0;
+            this._locationDelta.z = 0;
+
+            if(message.direction) this.object.setDirection(message.direction);
+
+            // Fall through to processMoveMessage
+        }
+        else if(this._isPredicting)
+        {
+            if(message.direction) this.object.setDirection(message.direction);
+            return;
+        }
+        else
+        {
+            if(message.direction) this.object.setDirection(message.direction);
+        }
+
+        // v9: For non-moving state, always sync location from server
+        if(this._locationDelta.length === 0)
+        {
+            if(message.location) this._location.assign(message.location);
+        }
 
         if(message instanceof ObjectMoveUpdateMessage) return this.processMoveMessage(message);
+
+        // Non-move message while standing still
+        if(this._locationDelta.length === 0)
+        {
+            if(message.location) this._location.assign(message.location);
+            this.object.setLocation(this._location);
+        }
+    }
+
+    private _trackMoveInterval(): void
+    {
+        if(this._lastMoveTime > 0)
+        {
+            const elapsed = this.time - this._lastMoveTime;
+
+            if(elapsed > 100 && elapsed < 1000)
+            {
+                this._measuredInterval = this._measuredInterval * 0.7 + elapsed * 0.3;
+            }
+        }
+
+        this._lastMoveTime = this.time;
     }
 
     private processMoveMessage(message: ObjectMoveUpdateMessage): void
     {
         if(!message || !this.object || !message.location) return;
 
-        this._changeTime = this._lastUpdateTime;
+        // v8.3: Skip zero-distance moves (standing still status updates)
+        const tgtLoc = message.targetLocation;
+        if(tgtLoc && message.location)
+        {
+            if(Math.abs(tgtLoc.x - message.location.x) < 0.01 &&
+               Math.abs(tgtLoc.y - message.location.y) < 0.01)
+            {
+                if(this._locationDelta.length === 0)
+                {
+                    this._location.assign(message.location);
+                    this.object.setLocation(this._location);
+                }
+                return;
+            }
+        }
+
+        // v9: Track actual server tick interval
+        this._trackMoveInterval();
+
+        // v9: Always use server position as start — prevents drift accumulation
+        this._location.assign(message.location);
+
+        this._changeTime = this.time;
 
         this._locationDelta.assign(message.targetLocation);
         this._locationDelta.subtract(this._location);
diff --git a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/pet/PetLogic.ts b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/pet/PetLogic.ts
index 1dc732f..bc703c9 100644
--- a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/pet/PetLogic.ts
+++ b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/logic/pet/PetLogic.ts
@@ -2,7 +2,7 @@ import { IAssetData, IRoomGeometry, IRoomObjectModel, MouseEventType, PetType, R
 import { RoomObjectMouseEvent, RoomObjectMoveEvent, RoomSpriteMouseEvent } from '../../../../../events';
 import { RoomObjectUpdateMessage } from '../../../../../room';
 import { PetFigureData } from '../../../../avatar';
-import { ObjectAvatarChatUpdateMessage, ObjectAvatarExperienceUpdateMessage, ObjectAvatarFigureUpdateMessage, ObjectAvatarPetGestureUpdateMessage, ObjectAvatarPostureUpdateMessage, ObjectAvatarSelectedMessage, ObjectAvatarSleepUpdateMessage, ObjectAvatarUpdateMessage } from '../../../messages';
+import { ObjectAvatarChatUpdateMessage, ObjectAvatarEffectUpdateMessage, ObjectAvatarExperienceUpdateMessage, ObjectAvatarFigureUpdateMessage, ObjectAvatarPetGestureUpdateMessage, ObjectAvatarPostureUpdateMessage, ObjectAvatarSelectedMessage, ObjectAvatarSleepUpdateMessage, ObjectAvatarUpdateMessage } from '../../../messages';
 import { MovingObjectLogic } from '../MovingObjectLogic';
 
 export class PetLogic extends MovingObjectLogic
@@ -36,7 +36,7 @@ export class PetLogic extends MovingObjectLogic
 
     public getEventTypes(): string[]
     {
-        const types = [RoomObjectMouseEvent.CLICK, RoomObjectMoveEvent.POSITION_CHANGED];
+        const types = [RoomObjectMouseEvent.CLICK, RoomObjectMouseEvent.MOUSE_ENTER, RoomObjectMouseEvent.MOUSE_LEAVE, RoomObjectMoveEvent.POSITION_CHANGED];
 
         return this.mergeTypes(super.getEventTypes(), types);
     }
@@ -210,6 +210,13 @@ export class PetLogic extends MovingObjectLogic
 
             return;
         }
+
+        if(message instanceof ObjectAvatarEffectUpdateMessage)
+        {
+            model.setValue(RoomObjectVariable.FIGURE_EFFECT, message.effect);
+
+            return;
+        }
     }
 
     public mouseEvent(event: RoomSpriteMouseEvent, geometry: IRoomGeometry): void
@@ -223,6 +230,12 @@ export class PetLogic extends MovingObjectLogic
                 break;
             case MouseEventType.DOUBLE_CLICK:
                 break;
+            case MouseEventType.ROLL_OVER:
+                eventType = RoomObjectMouseEvent.MOUSE_ENTER;
+                break;
+            case MouseEventType.ROLL_OUT:
+                eventType = RoomObjectMouseEvent.MOUSE_LEAVE;
+                break;
             case MouseEventType.MOUSE_DOWN: {
                 const petType = this.object.model.getValue<number>(RoomObjectVariable.PET_TYPE);
 
diff --git a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualization.ts b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualization.ts
index e12279a..ddce28e 100644
--- a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualization.ts
+++ b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualization.ts
@@ -1,18 +1,20 @@
+import { BLEND_MODES } from '@pixi/constants';
 import { Resource, Texture } from '@pixi/core';
-import { GetAssetManager, IGraphicAsset, IObjectVisualizationData, IRoomGeometry, RoomObjectVariable, RoomObjectVisualizationType } from '../../../../../api';
+import { AlphaTolerance, AvatarAction, AvatarSetType, GetAssetManager, IAvatarEffectListener, IAvatarImage, IGraphicAsset, IObjectVisualizationData, IRoomGeometry, RoomObjectVariable, RoomObjectVisualizationType } from '../../../../../api';
 import { AnimationData, AnimationStateData, DirectionData, LayerData } from '../data';
 import { FurnitureAnimatedVisualization, FurnitureVisualizationData } from '../furniture';
 import { ExperienceData } from './ExperienceData';
 import { PetVisualizationData } from './PetVisualizationData';
 
-export class PetVisualization extends FurnitureAnimatedVisualization
+export class PetVisualization extends FurnitureAnimatedVisualization implements IAvatarEffectListener
 {
     public static TYPE: string = RoomObjectVisualizationType.PET_ANIMATED;
 
     private static HEAD: string = 'head';
     private static SADDLE: string = 'saddle';
     private static HAIR: string = 'hair';
-    private static ADDITIONAL_SPRITE_COUNT: number = 1;
+    private static ADDITIONAL_SPRITE_COUNT: number = 11;
+    private static MAX_EFFECT_SPRITES: number = 10;
     private static EXPERIENCE_BUBBLE_VISIBLE_IN_MS: number = 1000;
     private static PET_EXPERIENCE_BUBBLE: string = 'avatar_addition_pet_experience_bubble';
     private static POSTURE_ANIMATION_INDEX: number = 0;
@@ -42,6 +44,10 @@ export class PetVisualization extends FurnitureAnimatedVisualization
     private _previousAnimationDirection: number;
     private _animationStates: AnimationStateData[];
 
+    private _effect: number;
+    private _effectAvatarImage: IAvatarImage;
+    private _effectSpriteCount: number;
+
     constructor()
     {
         super();
@@ -69,6 +75,10 @@ export class PetVisualization extends FurnitureAnimatedVisualization
         this._previousAnimationDirection = -1;
         this._animationStates = [];
 
+        this._effect = 0;
+        this._effectAvatarImage = null;
+        this._effectSpriteCount = 0;
+
         while(this._animationStates.length < PetVisualization.ANIMATION_INDEX_COUNT) this._animationStates.push(new AnimationStateData());
     }
 
@@ -115,6 +125,7 @@ export class PetVisualization extends FurnitureAnimatedVisualization
         super.update(geometry, time, update, skipUpdate);
 
         this.updateExperienceBubble(time);
+        this.updateEffectSprites(geometry);
     }
 
     protected updateExperienceBubble(time: number): void
@@ -225,6 +236,14 @@ export class PetVisualization extends FurnitureAnimatedVisualization
 
         if(!isNaN(color) && this._color !== color) this._color = color;
 
+        const effect = (model.getValue<number>(RoomObjectVariable.FIGURE_EFFECT) || 0);
+
+        if(effect !== this._effect)
+        {
+            this._effect = effect;
+            this.updateEffect(effect);
+        }
+
         this.updateModelCounter = model.updateCounter;
 
         return true;
@@ -541,11 +560,6 @@ export class PetVisualization extends FurnitureAnimatedVisualization
         return this.asset.getAssetWithPalette(name, paletteName);
     }
 
-    protected getAdditionalLayerCount(): number
-    {
-        return super.getAdditionalLayerCount() + PetVisualization.ADDITIONAL_SPRITE_COUNT;
-    }
-
     protected setLayerCount(count: number): void
     {
         super.setLayerCount(count);
@@ -592,6 +606,133 @@ export class PetVisualization extends FurnitureAnimatedVisualization
         return GetAssetManager().getTexture(name);
     }
 
+    private updateEffect(effectId: number): void
+    {
+        if(this._effectAvatarImage)
+        {
+            this._effectAvatarImage.dispose();
+            this._effectAvatarImage = null;
+        }
+
+        this._effectSpriteCount = 0;
+
+        if(effectId <= 0 || !this.data || !this.data.avatarManager) return;
+
+        const avatarImage = this.data.createAvatarImage('hd-180-1.ch-255-66.lg-280-110.sh-305-62', this._scale, 'M', null, this);
+
+        if(!avatarImage) return;
+
+        this._effectAvatarImage = avatarImage;
+
+        avatarImage.initActionAppends();
+        avatarImage.appendAction(AvatarAction.EFFECT, effectId);
+        avatarImage.endActionAppends();
+    }
+
+    private updateEffectSprites(geometry: IRoomGeometry): void
+    {
+        if(!this._effectAvatarImage) return;
+
+        this._effectAvatarImage.updateAnimationByFrames(1);
+
+        const effectSprites = this._effectAvatarImage.getSprites();
+        const effectStartIndex = this.totalSprites - PetVisualization.ADDITIONAL_SPRITE_COUNT;
+        const direction = this._effectAvatarImage.getDirection();
+        let spriteIndex = 0;
+
+        if(effectSprites)
+        {
+            for(const spriteData of effectSprites)
+            {
+                if(spriteData.id === 'avatar') continue;
+                if(spriteData.ink !== 33) continue;
+
+                const slotIndex = effectStartIndex + spriteIndex;
+
+                if(slotIndex < 0 || slotIndex >= this.totalSprites) continue;
+
+                const sprite = this.getSprite(slotIndex);
+
+                if(!sprite) continue;
+
+                const layerData = this._effectAvatarImage.getLayerData(spriteData);
+
+                let frameNumber = 0;
+                let offsetX = spriteData.getDirectionOffsetX(direction);
+                let offsetY = spriteData.getDirectionOffsetY(direction);
+                let dd = 0;
+
+                if(spriteData.hasDirections) dd = direction;
+
+                if(layerData)
+                {
+                    frameNumber = layerData.animationFrame;
+                    offsetX += layerData.dx;
+                    offsetY += layerData.dy;
+                    dd += layerData.dd;
+                }
+
+                if(this._scale < 48)
+                {
+                    offsetX /= 2;
+                    offsetY /= 2;
+                }
+
+                if(dd < 0) dd += 8;
+                else if(dd > 7) dd -= 8;
+
+                const assetName = ((((((this._effectAvatarImage.getScale() + '_') + spriteData.member) + '_') + dd) + '_') + frameNumber);
+                const asset = this._effectAvatarImage.getAsset(assetName);
+
+                if(!asset)
+                {
+                    sprite.visible = false;
+                    spriteIndex++;
+                    continue;
+                }
+
+                sprite.texture = asset.texture;
+                sprite.offsetX = ((asset.offsetX - (this._scale / 2)) + offsetX);
+                sprite.offsetY = (asset.offsetY + offsetY);
+                sprite.flipH = asset.flipH;
+                sprite.relativeDepth = -0.02;
+                sprite.alphaTolerance = AlphaTolerance.MATCH_NOTHING;
+                sprite.visible = true;
+
+                if(spriteData.ink === 33) sprite.blendMode = BLEND_MODES.ADD;
+                else sprite.blendMode = BLEND_MODES.NORMAL;
+
+                spriteIndex++;
+            }
+        }
+
+        this._effectSpriteCount = spriteIndex;
+
+        for(let i = spriteIndex; i < PetVisualization.MAX_EFFECT_SPRITES; i++)
+        {
+            const slotIndex = effectStartIndex + i;
+            if(slotIndex < 0 || slotIndex >= this.totalSprites) continue;
+
+            const sprite = this.getSprite(slotIndex);
+            if(sprite) sprite.visible = false;
+        }
+
+        if(spriteIndex > 0) this.updateSpriteCounter++;
+    }
+
+    public resetEffect(effect: number): void
+    {
+        if(effect === this._effect)
+        {
+            this.updateEffect(effect);
+        }
+    }
+
+    protected getAdditionalLayerCount(): number
+    {
+        return super.getAdditionalLayerCount() + PetVisualization.ADDITIONAL_SPRITE_COUNT;
+    }
+
     protected get data(): PetVisualizationData
     {
         return this._data as PetVisualizationData;
diff --git a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualizationData.ts b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualizationData.ts
index a1f92ec..9afca14 100644
--- a/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualizationData.ts
+++ b/node_modules/@nitrots/nitro-renderer/src/nitro/room/object/visualization/pet/PetVisualizationData.ts
@@ -1,16 +1,40 @@
-import { IAssetVisualizationData } from '../../../../../api';
+import { AvatarScaleType, IAvatarEffectListener, IAvatarImage, IAvatarImageListener, IAvatarRenderManager, IAssetVisualizationData } from '../../../../../api';
 import { AnimationSizeData, PetSizeData, SizeData } from '../data';
 import { FurnitureAnimatedVisualizationData } from '../furniture';
 
 export class PetVisualizationData extends FurnitureAnimatedVisualizationData
 {
     private _isAllowedToTurnHead: boolean;
+    private _avatarRenderer: IAvatarRenderManager;
 
     constructor()
     {
         super();
 
         this._isAllowedToTurnHead = true;
+        this._avatarRenderer = null;
+    }
+
+    public get avatarManager(): IAvatarRenderManager
+    {
+        return this._avatarRenderer;
+    }
+
+    public set avatarManager(renderer: IAvatarRenderManager)
+    {
+        this._avatarRenderer = renderer;
+    }
+
+    public createAvatarImage(figure: string, scale: number, gender: string = null, avatarListener: IAvatarImageListener = null, effectListener: IAvatarEffectListener = null): IAvatarImage
+    {
+        if(!this._avatarRenderer) return null;
+
+        let avatarImage: IAvatarImage = null;
+
+        if(scale > 48) avatarImage = this._avatarRenderer.createAvatarImage(figure, AvatarScaleType.LARGE, gender, avatarListener, effectListener);
+        else avatarImage = this._avatarRenderer.createAvatarImage(figure, AvatarScaleType.SMALL, gender, avatarListener, effectListener);
+
+        return avatarImage;
     }
 
     protected createSizeData(scale: number, layerCount: number, angle: number): SizeData
